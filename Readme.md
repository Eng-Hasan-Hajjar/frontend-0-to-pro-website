# Front-End Master Book — من HTML إلى JavaScript
موقع تفاعلي واحد متكامل لتعليم تطوير الويب من الصفر باللغة العربية، بديل عن عروض PowerPoint — إعداد **م. حسن حجار**.

## التشغيل
افتح `index.html` مباشرة في أي متصفح حديث. لأفضل تجربة مع المعاينة الحيّة وتشغيل الأكواد، شغّل خادماً محلياً بسيطاً بدل الفتح المباشر من القرص:
```bash
npx serve .
# أو
python3 -m http.server 8000
```

## الحالة الحالية — 32 من 32 جلسة
| المرحلة | الجلسات | الموضوع |
|---|---|---|
| **HTML &amp; CSS** | 01–08 | من مقدمة HTML حتى مشروع شامل + Git/GitHub Pages |
| **JavaScript** | 09–16 | من مقدمة JavaScript حتى مشروع ختامي شامل |
| **React &amp; Next.js** | 17–28 | من مقدمة React حتى النشر الفعلي على Vercel |
| **TypeScript** | 29–30 | الأساسيات، ثم Generics وUtility Types |
| **مشروع التخرّج** | 31 | دمج React وNext.js وTypeScript في مشروع واحد منشور فعلياً |
| **اختبار الكود** | 32 | Jest وReact Testing Library |

كل الجلسات مترابطة بأزرار "السابق/التالي" في تسلسل واحد متصل من 01 حتى 32.

> **ملاحظة عن الجلسات 18–32:** خلافاً لبقية الجلسات (المُستخرَجة من ملفات Word الأصلية)، هذه الجلسات مؤلَّفة يدوياً — كل جلسة بناءً على خطة الجلسة التي قبلها المُعلنة صراحة في صندوق "في الجلسة القادمة" (أو، بعد الجلسة 31 المفتوحة النهاية، بناءً على أنسب متابعة طبيعية)، وبنفس البنية والأسلوب التربوي تماماً.

## ملاحظة تصحيح إضافية (أكواد Jest/Node)
وسَّعنا فحص "قابلية التشغيل" ليستثني أيضاً أكواداً تستخدم رموز Jest العامة (test، expect، describe) أو CommonJS (require، module.exports) أو process.env من Node — هذه جميعها غير متاحة في متصفح عادي، فلا تحصل على زر تشغيل مضلِّل، تماماً مثل JSX وHooks وexport سابقاً.

## تحديث: إعادة هيكلة الصفحة الرئيسية (خريطة المسار)
الصفحة الرئيسية أصبحت الآن تعرض **خريطة مسار بصرية** (Roadmap) بالمراحل الخمس (HTML & CSS ← JavaScript ← React & Next.js ← TypeScript ← التخرّج والاختبار)، كل مرحلة تُظهر عدد الجلسات والساعات وما ستكون قادراً عليه بنهايتها. شبكة الـ32 جلسة أصبحت مُقسَّمة تحت عناوين المراحل بدل قائمة واحدة مسطَّحة، مع شريط تقدّم عام أعلى القسم. مصدر الحقيقة الوحيد لهذا التجميع هو مصفوفة `PHASES` في أعلى `script.js`.

## ملاحظة عن أكواد React (JSX)
أكواد React تحتوي JSX (خليط HTML داخل JavaScript) أو استخدام Hooks (مثل useState) — كلاهما لا يمكن تشغيله مباشرة عبر `new Function()` (يحتاج بيئة React كاملة). تكتشف الأداة تلقائياً هذه الحالات وتُصنِّفها:
- **JSX**: كتلة تحتوي وسوماً فعلية (`<div>`, `<Component />`)
- **React**: كتلة تستخدم Hook (مثل `useState(`) بلا وسوم JSX مباشرة
- كلتاهما تُعرَضان **بدون زر تشغيل** (لتفادي رسالة خطأ مضلِّلة)، بينما يبقى الزر يعمل بشكل طبيعي على كود JavaScript الخالص القابل للتنفيذ فعلاً.

## هيكل الملفات
```
index.html         الصفحة الرئيسية (بطل + شبكة الجلسات الـ16 + عن المدرب)
style.css          كل التنسيقات المشتركة بين المرحلتين
script.js          كل التفاعلات المشتركة (انظر أدناه)
lesson-01.html … lesson-08.html    جلسات HTML & CSS
lesson-09.html … lesson-16.html    جلسات JavaScript
```

## آليتا التفاعل (مهم لفهم الكود)
هذا الموقع يخدم نوعين مختلفين من المحتوى، ولكل منهما آلية تفاعل مناسبة، وكلتاهما موجودتان معاً في `script.js`:
- **جلسات HTML/CSS (01–08)**: زر **"👁 معاينة حيّة"** يعرض الكود مُصيَّراً فعلياً داخل `iframe` (`initPreviewButtons`, `buildPreviewDoc`).
- **جلسات JavaScript (09–16)**: زر **"▶ جرّب الكود"** ينفّذ الكود في بيئة معزولة ويعرض ناتج `console.log` (`runSandboxed`).

## ملاحظة تصحيح مهمة
كان هذا المستودع يحتوي سابقاً على ملفات المرحلتين مجتمعة، لكن بنسخة واحدة فقط من `script.js`/`index.html` (نسخة HTML/CSS) — ما جعل كل أزرار "جرّب الكود" في جلسات JavaScript (09–16) معطّلة تماماً بلا أي خطأ ظاهر، ومنع ظهور جلسات JavaScript في القائمة الرئيسية والشريط الجانبي. تم إصلاح ذلك ببناء `script.js` و`index.html` موحَّدين يدعمان الآليتين معاً، وبإعادة توليد الجلسات الـ16 بترقيم تنقّل متصل.

## قريباً
مسار **React** (بدءاً من الجلسة 17) قيد الإعداد — سيُضاف لاحقاً بنفس الجودة والمنهجية.

---
*Eng. Hasan Hajjar · Front-End Master Book*